export const TextAreaField =({label}:{label: string}) => {
    return (
        <fieldset>
            <label>
                {label}
            </label> 
               <textarea />

        </fieldset>
        
    );
}