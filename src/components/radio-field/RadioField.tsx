export const RadioField =({label}:{label: string}) => {
    return (
        
        <fieldset>
        <label>
            {label}
        </label> 
        <input type="radio"/>

    </fieldset>
    );
}